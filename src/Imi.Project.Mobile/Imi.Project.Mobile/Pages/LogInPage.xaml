<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:sfavatar="clr-namespace:Syncfusion.XForms.AvatarView;assembly=Syncfusion.Core.XForms"
             x:Class="Imi.Project.Mobile.Pages.LogInPage"
             x:Name="ThisPage">
    <ContentPage.Content>
        <StackLayout Padding="20" >
            <Frame BackgroundColor="LightSalmon" CornerRadius="50">
                <StackLayout Padding="20" Spacing="20">
              <Entry Text="{Binding UserName}" Placeholder="Username"></Entry>
            <Entry Text="{Binding Password}" Placeholder="Password" IsPassword="False"></Entry>
            <Button Command="{Binding LogInGetJwtToken}" CornerRadius="50" Text="log in"></Button>
              <Button CornerRadius="50" Text="Register"></Button>
                </StackLayout>
            </Frame>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>